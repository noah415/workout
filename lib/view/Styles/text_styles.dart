import 'package:flutter/material.dart';

const title1 = TextStyle(
  color: textColorFire,
  fontSize: 40,
  fontWeight: FontWeight.bold,
);

const whiteTitle = TextStyle(
  color: textColorFire,
  fontSize: 30,
  fontWeight: FontWeight.bold,
);

const whiteBody = TextStyle(color: textColorDark);

const cardColor = Color.fromRGBO(61, 64, 91, 1);

const textColorDark = Color.fromRGBO(135, 160, 178, 1);
const textColorFire = Color.fromRGBO(239, 100, 97, 1);
